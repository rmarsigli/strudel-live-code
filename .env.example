# WebSocket Server Port
# Used by both server and client
VITE_PORT=3001

# (Optional) Full WebSocket URL override
# Only set this if connecting to a different host or using custom protocol (wss://)
# VITE_WS_URL=ws://localhost:3001
